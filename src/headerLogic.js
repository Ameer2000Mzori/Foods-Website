// selecting elements
const headerIcons = document.querySelectorAll("header-Icons");

// gelobal variables

// functions
const changeIcons = () => {};

// events
window.addEventListener("load", changeIcons);
